import FormOnboarding from "./FormOnboarding";

const FavoritePlaces: React.FC = () => {
  return <FormOnboarding title="¿Qué tipo de lugares prefieres conocer?" />;
};

export default FavoritePlaces;
